version: '3.8'
services:
  app:
    build: .
    container_name: medical-telegram-warehouse-app
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
